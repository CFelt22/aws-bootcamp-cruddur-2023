#! /usr/bin/bash

ABS_PATH=$(readlink -f "$0")
echo $ABS_PATH
LAUNCH_PATH=$(dirname $ABS_PATH)
echo $LAUNCH_PATH
BIN_PATH=$(dirname $LAUNCH_PATH)
echo $BIN_PATH

source "$BIN_PATH/db/localdb"

source "$BIN_PATH/db/do-migrate"